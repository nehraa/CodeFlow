{
  "name": "codeflow",
  "version": "0.0.0",
  "private": true,
  "description": "CodeFlow monorepo — agentic IDE for code, blueprints, and version control. The pnpm workspace root; the actual packages live under packages/.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "secrets:scan": "gitleaks detect --source . --redact --config .gitleaks.toml",
    "secrets:scan:history": "gitleaks detect --source . --log-opts=--all --redact --config .gitleaks.toml",
    "secrets:install-hook": "git config core.hooksPath .githooks && echo 'Hooks installed. Pre-commit and pre-push will now run gitleaks.'"
  }
}
